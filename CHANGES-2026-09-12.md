# สรุปการแก้ไข — Flexozy

## 1. ระบบแจ้งเตือน (Toast)
- เขียนใหม่ `public/notify-standalone.js` เป็นระบบ toast โทนมืดสไตล์ sonner (rich colors, ปุ่มปิด, ปัดเพื่อปิด, ซ้อนกันได้, `toast.promise()`)
- คงชื่อฟังก์ชันเดิมทั้งหมด (`notify`, `notifyToast`, `notifyConfirm`, `notifyLoading`, `confirmAndSubmit`) — จุดเรียกใช้เดิมทั่วเว็บไม่ต้องแก้อะไรเลย
- เพิ่ม `window.toast(...)` / `toast.success/error/warning/info/loading/promise/dismiss` ให้ใช้แบบ sonner ได้ด้วย

## 2. ลิงก์ Vault รูปแบบใหม่
- เปลี่ยนจาก `https://flexozy.xyz/raw/vault/CODE` → `https://api.flexozy.xyz/CODE`
- Backend: เพิ่ม middleware แยกโดเมน `api.flexozy.xyz` ออกจากแอปหลักทั้งหมด (ไม่ผ่าน session/CORS/maintenance mode) รับเฉพาะ `GET /:code`
- Frontend: รวมจุดสร้างลิงก์เป็นฟังก์ชันเดียว `vaultRawUrl(code)`
- ลิงก์รูปแบบเก่ายังใช้งานได้ปกติ (ไม่พังของเก่าที่แชร์ไปแล้ว)
- **ต้องทำเอง**: เพิ่มโดเมน `api.flexozy.xyz` เป็นอีก Domain ในโปรเจกต์เดียวกันบนโฮสต์ (เช่น Vercel → Settings → Domains) แล้วชี้ DNS (CNAME) มาที่เดียวกับ `flexozy.xyz`

## 3. โลโก้ + พื้นหลัง
- เพิ่มโลโก้ที่ให้มาไว้ที่ `public/images/flexozy-logo.png` ใช้เป็น favicon, โลโก้แถบเมนู, และโลโก้หน้า login
- พื้นหลังทั้งเว็บ: เปลี่ยนจากสีดำล้วนเป็นไล่เฉดสี + แสงเรือง (glow orb) โทนน้ำเงิน-ฟ้าตามโลโก้ พร้อมลายกริดจางๆ (`.app-bg-fx` ใน `style.css`)

## 4. บั๊กจริงที่พบและแก้ในระบบเงิน/ร้านค้า/โค้ดแลกรางวัล
พบช่องโหว่ **race condition** เดียวกันในทั้ง 3 ระบบ: `db.js` เดิมอ่าน-แก้-เขียนข้อมูลโดยไม่มีการล็อกใดๆ บน Vercel serverless
ถ้ามี request 2 อันมาพร้อมกัน (กดรัว/ยิง API ซ้ำ) จะเครดิตเงินซ้ำ/แลกโค้ดซ้ำ/ตัดสต็อกไม่ตรงได้
- **แก้ที่ราก**: เพิ่ม distributed lock (Redis `SET NX PX`) ใน `mutate()` (`db.js`) บังคับให้ทุกการแก้ข้อมูลทำทีละครั้งจริงๆ ครอบคลุมทุกระบบที่ใช้ `mutate` (เติมเงิน, ร้านค้า, โค้ดแลกรางวัล, vault, ฯลฯ)
- เพิ่มการเช็คกันแลกซองอั่งเปาซ้ำอีกชั้นใน `wallet.controller.js` (เทียบ ref เดิมก่อนเครดิต)
- เพิ่ม defensive check กรณีหา user ไม่เจอตอนสั่งซื้อ (`store.controller.js`)

## 5. ความปลอดภัยหน้าแอดมิน ("ป้องกันขั้นสูงสุด")
- **[Critical] แก้ช่องโหว่ session forgery**: เดิมถ้า deploy โดยไม่ตั้ง `SESSION_SECRET` (หรือใช้ค่า default/สั้นเกินไป) ใครก็ปลอมคุกกี้ session ให้ตัวเองเป็นแอดมินได้ทันที เพราะ `is_admin` เก็บอยู่ในคุกกี้ที่เซ็นด้วยค่านี้ → ตอนนี้เซิร์ฟเวอร์จะ **ปฏิเสธการสตาร์ท** ทันทีถ้ารันบน production ด้วยค่าที่ไม่ปลอดภัย
- **แก้สิทธิ์ค้าง**: `requireAdmin`/`hasPermission`/`requireStaff` เดิมเชื่อ `session.user.is_admin` ที่ cache ไว้ตอน login (คุกกี้อยู่ได้ 7 วัน) — ตอนนี้เช็คกับ `ADMIN_DISCORD_IDS` สดทุก request ถอดชื่อออกจาก ENV แล้ว redeploy = ตัดสิทธิ์ทันที ไม่ต้องรอ session หมดอายุ
- **เพิ่ม audit log**: ทุกคำขอที่แก้ไขข้อมูล (POST/PUT/PATCH/DELETE) จากบัญชีแอดมิน/ทีมงาน ถูกบันทึกไว้ ดูได้ที่แท็บใหม่ "Audit Log" ในหน้าแอดมิน (แอดมินเต็มเท่านั้น)
- **เพิ่ม security headers** ผ่าน `helmet` (กัน clickjacking, MIME sniffing ฯลฯ)
- **เพิ่ม 2 ชั้นป้องกันแบบเปิด/ปิดได้เอง (ไม่บังคับ)** ผ่าน env var:
  - `ADMIN_ALLOWED_IPS` — จำกัด IP ที่เข้าหน้าแอดมินได้
  - `ADMIN_PANEL_PIN` — รหัส PIN ชั้นที่สอง ต้องกรอกก่อนเข้าหน้าแอดมิน (มี popup ขึ้นอัตโนมัติที่หน้าเว็บ)

**ต้องทำเอง**: ตั้งค่า `SESSION_SECRET` ใหม่เป็นค่าสุ่มปลอดภัย (ดูวิธีใน `.env.example`) ก่อน deploy — ถ้าไม่ตั้ง เซิร์ฟเวอร์จะไม่ยอมสตาร์ทเลยตอนอยู่บน production (ตั้งใจให้เป็นแบบนี้เพื่อความปลอดภัย)

## 6. ติดตั้ง dependency ใหม่
เพิ่ม `helmet` ใน `package.json` — รัน `npm install` ก่อน deploy รอบถัดไป

## 7. แยกหน้าแอดมินออกจากเว็บหลัก (real /admin.html)
- ย้ายแผงแอดมินทั้งหมดออกจาก `index.html` ไปเป็นไฟล์จริงแยกต่างหาก `views/admin.html`
- ไฟล์จริงเก็บไว้ **นอก** โฟลเดอร์ `public/` (ที่ `views/`) ทำให้ `express.static` เสิร์ฟไม่ได้แม้แต่ทางอ้อม
- เพิ่ม route `GET /admin.html` ใน `app.js` ที่เช็คสิทธิ์ก่อนเสมอ: คนทั่วไป/คนไม่ได้ล็อกอิน จะได้ **404 ธรรมดา** (ไม่ใช่ 403) เพื่อไม่ให้รู้ด้วยซ้ำว่ามีหน้านี้อยู่ — ต้องเป็นแอดมินเต็มหรือทีมงานที่มีสิทธิ์บางอย่างเท่านั้นถึงจะได้ไฟล์จริงกลับไป
- แยกโค้ด JS ของทั้งเว็บ (ที่เดิมฝังอยู่ใน `<script>` inline ของ index.html) ออกมาเป็น `public/main.js` ไฟล์เดียว ให้ทั้ง `index.html` และ `admin.html` โหลดใช้ร่วมกัน (โค้ดชุดเดียวกันเป๊ะ ไม่มีอะไรซ้ำซ้อน/ไม่ตรงกัน)
- ปุ่ม "Admin" ในเมนู, และปุ่มลัดต่างๆ ในโปรไฟล์ ตอนนี้พาไปที่ `/admin.html` จริงๆ (ไม่ใช่แค่เปลี่ยน tab ในหน้าเดิม)
- ทดสอบแล้วว่า: ไม่ได้ล็อกอิน → 404, ล็อกอินแต่ไม่ใช่แอดมิน/ทีมงาน → 404, เป็นแอดมิน/ทีมงาน → ได้หน้าเต็มตามปกติ

**หมายเหตุ**: โค้ด JS ของฟังก์ชันแอดมิน (เช่นชื่อฟังก์ชัน endpoint ที่เรียก) ยังอยู่ใน `main.js` ที่โหลดให้ทุกคน (จำเป็นเพราะไฟล์เดียวกันต้องรันหน้าเว็บหลักด้วย) — สิ่งที่ซ่อนจริงคือ **โครงสร้าง HTML/UI ของแผงแอดมิน** ซึ่งเป็นสิ่งที่ขอไว้ ถ้าต้องการซ่อนโค้ด JS แอดมินด้วย (แยกเป็นไฟล์ JS ของตัวเองไม่ปนกับหน้าเว็บหลักเลย) เป็นงานเพิ่มเติมระดับ "แบบเต็ม" ที่ยังไม่ได้ทำ

---

## ยังไม่ได้ทำ (ต้องคุยรายละเอียดเพิ่ม)
- **Reskin ทั้งเว็บแบบเต็มรูปแบบ**: ตอนนี้เว็บผสมกันระหว่างการ์ดสีขาว/เทาอ่อนกับพื้นหลังเข้ม ถ้าต้องการทำเป็นธีมมืดล้วนทั้งเว็บ (ปรับการ์ด/ข้อความ ~50 จุดใน index.html) แจ้งมาได้ จะแก้ต่อให้
- **จุดที่ยังคิดว่า "พัง" ในระบบเติมเงิน/ร้านค้า/เมนูแอดมิน** ที่นอกเหนือจากบั๊ก race condition ที่เจอ — ถ้ามีอาการเฉพาะ (error message, ปุ่มไม่ทำงาน, ฯลฯ) บอกมาได้เลยจะตามแก้ต่อ
