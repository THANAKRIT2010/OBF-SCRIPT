import crypto from"node:crypto";export function getApiKey(e){const t=e.headers.authorization||"";return t.toLowerCase().startsWith("bearer ")?t.slice(7).trim():e.headers["x-api-key"]||""}export function requireApiKey(e,t){const r=getApiKey(e),n=process.env.API_ADMIN_KEY||"";return r&&n&&r.length===n.length&&crypto.timingSafeEqual(Buffer.from(r),Buffer.from(n))||(t.status(401).json({error:"unauthorized"}),!1)}