export const metadata = {
  title: "Flexozy API",
  description: "Flexozy API Gateway documentation"
};

export default function RootLayout({ children }) {
  return (
    <html lang="th">
      <body>{children}</body>
    </html>
  );
}
